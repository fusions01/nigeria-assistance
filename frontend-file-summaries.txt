{
  "frontend/src/index.css": "Global styles with Nigeria-themed OKLCH color system using green as primary color, custom font imports (Plus Jakarta Sans), and design tokens for both light and dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Nigeria-themed colors, Plus Jakarta Sans font family, custom border radii, card shadows, and fade-in animation.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations — fixes `useRequestMoney` to call the correct `actor.submitMoneyRequest()` method (instead of the non-existent `actor.requestMoney()`), adds the required `email` parameter resolved from sessionStorage via a `getSessionEmail()` helper, and adds a `getSessionEmail()` utility that reads from `emailAuth` and `userSession` sessionStorage keys supporting both plain-string and JSON formats.",
  "frontend/src/App.tsx": "TanStack Router setup with isUserAuthenticated() helper that checks emailAuth, isLoggedIn, and userSession sessionStorage keys; all protected user routes use requireAuth and admin routes use requireAdmin checking isAdmin=true in sessionStorage.",
  "frontend/src/components/Layout.tsx": "Full-page layout with sticky header — fixed `isLoggedIn` detection to check `!!emailAuth` (truthy check on the stored JSON string) instead of `sessionStorage.getItem('isLoggedIn') === 'true'`, so email/password and phone OTP users see the correct navigation links.",
  "frontend/src/components/ProfileSetupModal.tsx": "Three-step registration wizard modal (Phone → Name → Email) that uses `useSaveProfile` (instead of `useSaveCallerUserProfile`) so the mutation receives the correct partial parameter shape matching the `saveProfile` backend function, fixing the TypeScript type mismatch.",
  "frontend/src/components/BankAccountLookup.tsx": "Bank account verification component that lets users select a bank and enter a 10-digit account number to look up the account holder name via Paystack. Removed the misleading \"Internet Identity\" error message — unauthorized errors now show a generic session-expiry message instead, so email/password and phone OTP users are no longer told to use Internet Identity.",
  "frontend/src/components/PhoneVerification.tsx": "Two-step phone verification component with Nigerian number validation (+234/0xx format), OTP generation via backend with on-screen code display, and 6-digit OTP input using InputOTP.",
  "frontend/src/pages/Home.tsx": "Landing page with hero, features, how-it-works, supported banks, and CTA sections — now includes a visible \"Admin Login\" link at the bottom of the page accessible to all visitors that navigates to /admin/login.",
  "frontend/src/pages/Dashboard.tsx": "User dashboard page — now imports `isCanisterStoppedError` and displays a friendly Alert banner instead of raw error text when the money requests query fails due to a stopped canister; also exposes the `error` state from the query hook.",
  "frontend/src/pages/SendMoney.tsx": "Two-step send money form — fixes the TypeScript error by updating the `BankAccountLookup` usage from the old `onAccountConfirmed`/`onReset` props to the new `onVerified(bank, accountNumber, accountName)` callback signature.",
  "frontend/src/pages/TransactionHistory.tsx": "Transaction history page — now imports `isCanisterStoppedError` and shows a friendly Alert with the unavailability message instead of raw error text when the transaction history query fails due to a stopped canister.",
  "frontend/src/pages/Profile.tsx": "Read-only user profile page — now imports `isCanisterStoppedError` and displays a friendly Alert instead of raw error details when the profile query fails due to a stopped canister; also fixes the `isLoggedIn` check to include `!!emailAuth`.",
  "frontend/src/lib/utils.ts": "Utility functions including cn(), formatNGN(), formatDate(), formatTimestamp(), detectDeviceType(), and the new exported `isCanisterStoppedError(error)` function that detects IC0508/canister-stopped errors across various error shapes, plus a shared `SERVICE_UNAVAILABLE_MSG` constant.",
  "frontend/index.html": "HTML entry point with NigeriaPay title, favicon pointing to generated logo, and Google Fonts preconnect links.",
  "frontend/src/pages/AdminLogin.tsx": "Admin login page that uses `window.location.href` for a hard navigation to `/admin/dashboard` after setting `isAdmin=true` in sessionStorage, ensuring the route guard reads the flag correctly on the new page load.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin dashboard with four tabs — now imports `isCanisterStoppedError` and uses a `getErrorMessage()` helper to display friendly unavailability messages for all query errors (profiles, requests, transactions) and mutation errors (fulfill, send money); adds dedicated `sendError` and `fulfillError` state with Alert display.",
  "frontend/src/components/EmailVerification.tsx": "Two-step email verification component — updated `EmailVerificationProps` to accept an optional `email` prop (pre-filled, read-only) so callers that already collected the email can pass it in; when `email` is pre-filled the component skips straight to the code step.",
  "frontend/src/pages/RequestMoney.tsx": "Authenticated money request form — updated to extract the user's email from sessionStorage via `getSessionEmail()` and pass it to the `requestMoney` mutation so email/password and phone OTP users can submit without triggering an Internet Identity popup; also improves error messages for profile-not-found and missing-principal backend errors.",
  "frontend/src/pages/Signup.tsx": "Multi-step signup page (Phone → Details → Email → Done) — removed the unsupported `buttonLabel` and `verifyLabel` props from the `PhoneVerification` component usage, passing only the required `onVerified` callback.",
  "frontend/src/pages/Login.tsx": "Login page with Email/Password and Phone OTP tabs — removed the unsupported `buttonLabel` and `verifyLabel` props from the `PhoneVerification` component usage, passing only the required `onVerified` callback."
}